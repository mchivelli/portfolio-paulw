# Production Environment Variables
# Copy this file to .env in production and fill in your values

# Mailgun Configuration (Required for email functionality)
MAILGUN_API_KEY=your-production-mailgun-api-key
MAILGUN_DOMAIN=your-production-domain.com

# Server Configuration
PORT=3001
NODE_ENV=production

# Security (Optional - for enhanced security in production)
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=100  # Max requests per window

# Production Notes:
# 1. Use your verified domain instead of sandbox for production
# 2. Keep your API key secure and never commit it to version control
# 3. Consider using environment-specific configuration management
# 4. Monitor email delivery and set up alerts for failures